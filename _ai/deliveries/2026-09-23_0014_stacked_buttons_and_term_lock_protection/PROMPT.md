# Suno 音乐通 · 审查交接说明 (Handoff & Review Brief)

> **致审查方 / 审核人**：
> 本交付结果汇总了近期的全量修改成果，重点落实了以下两项关键要求：
> 1. **“上英下中”垂直双行紧凑布局策略（用户明确要求）**：针对 `+ Audio`、`+ Voice`、`+ Inspo` 紧凑按钮，按钮内部按上面英文、下面中文小字排版，在按钮内部自闭环换行，严禁向外部网格容器注入兄弟节点，彻底消除按钮溢出被裁切风险。
> 2. **谷歌动态翻译分段隔离与术语锁定保护**：针对用户提出的“整句机翻会不会把四四拍正拍律动盖掉”的关键问题，重构为**分段隔离锁定管线**。本地专业词库命中的段落物理隔离、绝不送给谷歌；仅送未知片段，彻底杜绝专业词被机翻冲垮。

---

## 审查重点项自检
- [x] **上英下中排版**：`+ Audio 添加音频`、`+ Voice 加入一段声音`、`+ Inspo 从歌单里找感觉` 三键并排整齐，双行垂直居中，100% 完整显示。
- [x] **四四拍锁定保护**：即使后面跟着生僻未知词，`四四拍正拍律动`、`细腻指弹` 绝对锁定不被谷歌机翻破坏。
- [x] **词库 0 括号**：73 个核心词条 `zh_name` 无 `()`、`[]`，无 `[中文]` 占位符。
- [x] **做第一首歌向导**：抽屉置顶 6 步做歌指引，仅第 3、4 步各 1 个复制按钮。
- [x] **自动化测试**：14 项断言 100% 通过（`node scripts/test_dom_behavior.js`）。

---

## 快速体验方式
- **Chrome 扩展**：`chrome://extensions/` ➔ 加载已解压的扩展程序 ➔ 选择 `dist/chrome-extension/`。
- **油猴脚本**：导入 `dist/suno-copilot.user.js`。
